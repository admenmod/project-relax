// const { os } = await require('os');
// const { radar } = await require('radar');
//
//
// os.process = dt => {
// 	if(0) setRule('move to target', false);
// };
//
//
// radar.on('detect', o => {
// 	if(o.type === 'item') os.cmd('give items');
// });
